import React from 'react';

interface Props {
  date: Date;
  setDate: (date: Date, refetch: any) => void;
  lastUpdated: String;
}

const DashboardTable = ({date, setDate, lastUpdated}: Props) => {
  
}

export default DashboardTable;